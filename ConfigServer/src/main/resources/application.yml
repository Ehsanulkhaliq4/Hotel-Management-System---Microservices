server:
  port: 8085
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ehsanulkhaliq4/micro-configuuration-service
          clone-on-start: true
